eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8800
spring:
  application:
    name: eureka-ribbon-client

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream